<div>
    <% if(rs.length > 0) { %>
    <table border="1">
        <tr><th>Name</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/company/?companyName=<%= rs[i].companyName %>"> <%= rs[i].companyName %> </a></td>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    No schools are entered into the education database table.
    <% } %>
</div>

<div style="margin-top:10px;">
    <a href="/company/create">Create New Account</a>
</div>